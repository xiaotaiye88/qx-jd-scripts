# 北京移动签到 + 网龄成长计划

适配**北京移动**（签到赢好礼 + 网龄成长计划领流量），基于抓包逆向实现。其它省份的签到活动接口不同，不通用。

## 功能

| 功能 | 类型 | 说明 |
|------|------|------|
| 签到赢好礼 | rewrite + task | 打开 App 签到页时钩子自动捕获活动 token，每天 9:00 定时签到并推送结果，token 失效自动提醒续期 |
| 网龄成长计划 | rewrite + task | 打开网龄页自动登录，检测当月 1GB 流量包是否已领取；未领取时可全自动领取：发短信验证码 → ntfy 读码 → 加密提交（实验功能，可关闭退化为提醒） |

逆向说明：网龄领取的短信确认接口 `templatePrize` 采用 `AES-128-ECB(随机密钥)` 加密请求体 + `RSA-2048` 加密密钥放 `coc-aurora` 头的方案（`coc-aurora-v: 2`），脚本内嵌纯 JS 实现。App 页面提交前还会走一个被 WASM 加密的 `templatePrize/new` 创建订单，脚本跳过它直接提交；若服务端强校验 `/new` 订单则自动领取会失败并推送提醒，届时手动领取即可（钩子仍会完成当月状态检测）。

## 快速开始

### 1. 添加重写订阅（抓凭证必需）

圈X → 风车 → **重写** → 订阅 `+`：

```
https://raw.githubusercontent.com/xiaotaiye88/qx-jd-scripts/master/bj10086.conf
```

> MITM 需安装并信任证书；`[mitm] hostname` 已包含在 conf 内：`h5.bj.10086.cn, dev.coc.10086.cn`

### 2. 添加任务订阅

圈X → 风车 → **任务** → 订阅 `+`：

```
https://raw.githubusercontent.com/xiaotaiye88/qx-jd-scripts/master/tasks/bj10086-tasks.json
```

含两条任务：`移动签到`（每天 9:00）和 `移动凭证接力`（每 15 分钟，仅在打开过 App 页面后才实际干活）。

### 3. 添加 BoxJs 订阅（自动领取需要）

BoxJs → 订阅 → 添加：

```
https://raw.githubusercontent.com/xiaotaiye88/qx-jd-scripts/master/boxjs/bj10086.boxjs.json
```

在「北京移动签到 → ntfy 短信主题地址」填入你的 ntfy 主题地址（形如 `https://ntfy.sh/你的主题`，需与手机短信转发到 ntfy 的目标主题一致）。

⚠️ **ntfy 主题名即读取密码**：主题里流的是你的短信验证码，务必使用随机长主题名，不要把真实主题写进任何公开脚本/配置。

### 4. 激活凭证

开着圈X，打开中国移动 App：
- 进「签到赢好礼」页面 → 自动捕获签到 token
- 进「网龄成长计划」页面 → 自动捕获 User-Token / userId

之后无需手动干预；凭证失效时按通知提示重开对应页面即可。

## 工作原理

```
打开 App 签到页/网龄页（圈X MITM 开启）
        │ rewrite 钩子捕获凭证（请求原样放行）
        ▼
task 每15分钟接力 / 每天9:00 每日任务
  1. 签到: doPrize?type=sign（明文接口）
  2. 网龄: activityOrderListNew 查当月是否已领
          未领 → user/smsCode 发验证码 → ntfy 轮询取码
               → templatePrize（AES+RSA 加密提交）
        ▼
$notify 推送结果
```

## 已知限制

- 网龄 `activityId`（脚本内 `16096`）可能随月份/期数变化，领取报活动错误时需抓包更新；
- 自动领取是实验功能，失败时优雅降级为提醒，不影响检测和签到；
- 需要 iOS 14+（脚本使用 BigInt）。
